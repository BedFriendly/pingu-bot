# ==============================
# Pingu Bot - Production Environment Variables
# ==============================
# Railway 배포 시 Railway Dashboard에서 설정하세요.
# 이 파일은 참고용이며, 실제 값을 커밋하지 마세요!

# Discord 설정 (필수)
DISCORD_TOKEN=your_production_bot_token
DISCORD_CLIENT_ID=your_production_client_id

# 데이터베이스 (Railway PostgreSQL 사용 시 자동 설정됨)
# SQLite 사용 시 (초기 개발):
DATABASE_PATH=/app/data/pingu.db
# PostgreSQL 사용 시 (프로덕션):
# DATABASE_URL=postgresql://user:password@host:port/database

# 애플리케이션 설정
NODE_ENV=production
LOG_LEVEL=info

# External APIs (선택사항)
UNSPLASH_ACCESS_KEY=your_unsplash_access_key

# Railway 자동 설정 변수 (참고용)
# RAILWAY_ENVIRONMENT=production
# RAILWAY_GIT_COMMIT_SHA=abc123
# RAILWAY_GIT_BRANCH=main
